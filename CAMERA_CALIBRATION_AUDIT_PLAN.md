# Camera calibration：审计与执行计划

审计日期：2026-09-09。对象：`README_camera_calibration.md`（含 09-14 周计划），并静态核对相邻 `Field_2026_Social/preprocessing` 中的标定、坐标转换、抽帧、合并代码和现存配置。本轮没有检查现场、读取录像或运行拟合；以下误差是已有 JSON 的训练拟合误差，不是本轮测得的精度。

## 结论

保留统一场地坐标、分相机模型、闭合录像取材和事件台账。执行顺序改为：核实几何与像素坐标 → 准备验证工具 → 现场采集 → CH03 试点 → 其他相机 → 独立验收 → 时间对齐和轨迹融合。不能承诺一次 30 分钟现场操作或指定周五就能让 8 台全部达标。

## 1. 审计发现

| 优先级 | README 位置 / 证据 | 问题和必须采取的动作 |
|---|---|---|
| P1 | Phase 1、周一的“网格吻合即可沿用” | CH05 旧 JSON 的四个 world_points 中心是 (457.2,304.8) cm，而当前 layout 的 left.center_cm 是 (342.6,304.8)。x 差 114.6 cm。原因未证实；核对历史布局、对应日期和实测位置，不能自动 refit 或沿用。旧参数必须绑定旧几何版本。 |
| P1 | Phase 1 增加杆脚；周三 pole base + wall-top → homography | 一个地面 homography 只能使用同一地面上的点。墙顶 z=97.79、屋顶、地板和杆脚不能随意混合。先识别每个角点实际高度；非共面点仅用于正确的 3D–2D pose 方法。 |
| P1 | Phase 3 “3 poles × 2 solves pose” | 共线杆脚加同高墙顶构成一个竖直平面，并非非共面点，也不保证稳定唯一位姿。PnP 要有可靠内参、畸变处理和解选择。当前 calibration.py 的 PnP 分支仍调用 intrinsics_from_specs，并跳过已有内参的去畸变路径；与文档“棋盘格后 PnP”不一致。主线改用棋盘格 + 分散地面点。 |
| P1 | common frame / inventory | layout 注释也包含占位信息：CH03/04 pos 在 B1/B3，README 却写位于场地两端；CH01/02 pole 名与 pos_cm 也不一致。不能把所有字段当成已测量真值。M1/M5/N1/N5 被 layout 明确标作估计值，不能用于厘米级验收。 |
| P1 | Phase 4 箱内角点 → shelter footprint | 室内净尺寸不等于室外 footprint。需要室内原点相对场地的位置、朝向、偏移和地板高度；只测宽长不够。CH07/08 的实际型号、视图、左右箱对应关系也需要静帧/现场确认。 |
| P1 | Phase 5 / Friday acceptance | 现有 reproj_rmse_cm 是拟合点误差。用同一批 held-out 点选模型后，又当最终验收，仍有选择偏差。需要训练、选模验证、最终测试三个按空间位置分开的集合。 |
| P2 | CH01/02 status 和 Phase 2 | 实际各有20点 poly，拟合 RMSE 55.88/51.30 cm；“not done”应解释为未验收，不是无参数。6点只是二阶模型求解下限，不能据此证明泛化；应比较分区模型，并限制有效区域。 |
| P2 | CH03/04 status | 已有9/8点 poly，拟合 RMSE 19.694/11.28 cm；点集中在端部且含估计坐标，不能据此宣布远场达标。 |
| P2 | 周三“新写 plumb-line script” | 当前 intrinsics.py 已有 --plumb。无需重复开发；该路线只作为对照，不把墙线变直等同于地面准确。 |
| P2 | Phase 6 bbox bottom-center | 仅是动物位置近似；俯视图底边没有特殊地面接触意义。先定义动物位置为躯干中心投影或其他统一解剖参考点，再按视角验证估计器。不能把高度校正对所有 bbox 点一律套用。 |
| P2 | WISER、calibration walk 和 time alignment | 文件名只提供时间锚点，不能保证曝光时间同步；需保存片段裁切偏移和 PTS，并验证时区、设备偏移、漂移。OSD 的 PC−1h 是历史观察，不是永久常数。WISER 自身有误差，不能单独代表标定真值。人站杆旁也不等于已知精确脚点，箱内相机看不到场外 walk。 |
| P2 | drift 和 epoch | 5 px 在不同区域对应不同厘米误差。使用多处稳定参考点、匹配置信度与局部厘米变化；受碰撞后先置 suspect，从实际确认时间起恢复有效，不能统一写死 09-14 valid_from。 |
| P2 | 单日方案步骤5 | 调整麦克风 USB 与相机标定无关且会中断采集，移出本任务。闭合文件抽帧也不是“zero impact”：只做短窗口、低负载读取，重计算放分析机。 |

另：`merge_cameras.py` 实际在 `preprocessing/data_merging`，不在 computer_vision。它保留 camera:track_id，尚未解决跨相机身份；其 absolute-time 在文件名无法解析时退回0，验收时必须拒绝这种情况，不能仅检查合并文件生成成功。

## 2. 开工前：桌面准备（预计半天至一天）

执行者：分析端开发者；现场负责人提供测量和历史事件信息。

1. 建立本次 session 文件夹，保存旧配置副本，不覆盖六月参数。逐台列出型号、实际视图、镜头/夜间模式、分辨率、旋转、裁切、缩放、覆盖区域。
2. 指定每台的闭合源文件和帧时间；不要用“最新片段”代替明确 session。记录源文件名、帧 PTS、裁切起点、输出图尺寸、旋转/缩放处理。intrinsics 和标记点必须处于一致像素空间。
3. 对比 CH05/06 保存的 world_points 与 layout，整理几何差异表。复核 A0 原点、轴向、杆位、两箱位置和室内原点。测量应记录方法、日期和不确定度，不能将设计网格无条件视为实测网格。
4. 先实现或补齐离线验证报告，再开始选模型：按点ID划分 train/validation/test；显示厘米残差箭头、空间覆盖和有效区域；拒绝共线/病态点集、混合高度和缺失必需内参。
5. 本次沿用已有棋盘格检测器。建议10×7格、每格75 mm，板面750×525 mm，硬质平整、哑光、不缩放打印；实际量格长。这对应9×6内角点，运行时明确 --pattern 9x6 --square 7.5（代码单位cm），不要使用当前默认格长或 auto 小棋盘候选。板子是否足够大，以实际检测预检为准。

开工关卡：已明确要标定的平面、已知坐标来源、旧新几何冲突处理方案，并能用独立点生成误差报告。无法确认的字段写 unknown，不填推测值。

## 3. 一次现场访问：预留半天，允许补拍

实际动手预计90–150分钟，另外预留等待片段闭合和检查时间；这是排期估计，不是保证。选择允许布置临时物体的动物空场/换组时段。若当日不能进入箱内，箱内标定单列 pending。

物品：硬质棋盘板、长卷尺和短尺、编号高对比平面标记、记录表；可选已登记的 UWB 标签。地面标记平放并稳定，不把草尖或竖起牌面当成地面。

### A. 几何核对与地面点（45–75分钟）

- 核对轴向、场地尺寸、箱体位置，重点解决 CH05 的114.6 cm差异。测量目标不确定度建议：箱内/箱边≤0.5 cm，场地控制点≤2 cm；做不到则记录实际值并反映到误差解释中。
- 初始布置候选为5×5位置网格：x约为场地长度的10/30/50/70/90%，y约为宽度的10/30/50/70/90%。它只是布点草图，最终坐标必须实测；避开障碍，并根据每台实际视野增补。
- 每台 CH01–04 目标至少24个可见、空间分散的位置：12训练、6选模验证、6最终测试；点不足就补测或缩小有效区域。接缝两侧、边缘、近中远都要有验证覆盖，不能全在边界杆脚。
- 标记不必同时全部放置；可逐点放同一编号目标，每点保持5–10秒并记录时间、实测坐标、平面和ID。提前标好三个集合，所有相机共享同一位置的集合归属，避免泄漏。

### B. CH03/04 内参（每台10–15分钟）

- 相机保持原位。在安全可达范围内改变棋盘距离、左右上下位置与倾角，停稳1–2秒；目标每台20–30个清晰、不同姿态的有效视图，而不是只录够90秒。
- 覆盖目标使用区域及可达图像边缘；不要冒险为到达高处像素而攀爬。预留至少5个额外姿态作内参检查。若角点太小或模糊，调整板尺寸、距离或采集条件后补拍。
- 先完成CH03并检查一段已闭合录像的检测质量，再复制到CH04，减少整批失败风险。

### C. CH05–08 平面标定（20–40分钟）

- 确定每台实际看到的平面：地板、屋顶、室外地面分别处理；如同台需覆盖多个高度，建立不同平面模型和区域mask。
- 每个目标平面建议12个已测位置：6拟合、3选模验证、3最终测试，覆盖四周和内部。若点数不足，可先出 provisional 结果，但不能用四角训练误差宣告通过。
- 对箱内记录净宽长、地板高度、室内坐标原点及朝向到场地的变换。拍有编号/尺子的静态画面，确认不是把外墙角当内墙角。

### D. 离场前检查

- 等待实际出现闭合 `_to_` 文件，再轻量抽取所需窗口；整点并不保证每路已完成关闭。
- 检查两台棋盘角点可检、控制点清晰、各集合覆盖足够、箱内尺寸和坐标已记录。失败材料在现场补拍。
- 可选 UWB 静态停留：标签放在已测位置，记录天线参考点及高度，每点停留10–20秒。这是系统一致性检查，不能替代地面独立测试；不要求箱内相机看到场外步行。

## 4. 离线拟合顺序（预计2–3个工作日）

1. **CH03试点**：棋盘内参 → 地面点去畸变 → homography。不混入墙顶。先检查像素空间和独立位置误差，再处理CH04。plumb-line只作备选比较；PnP待内参/畸变代码路径修正后才可评估。
2. **CH05/06及CH07/08**：按确认的目标平面拟合，先输出局部平面坐标，再转场地坐标；近轴玻璃影响是否可忽略由独立点误差决定，不预设完全被homography吸收。
3. **CH01/02**：重算当前二阶poly作为基线；比较带归一化的二阶poly与基于足够密集控制点的分区映射（例如三角网内分片仿射）。每区要求覆盖及非退化几何，限制到控制点覆盖区域；接缝附近单独验证。TPS/更高阶仅在数据支持时考虑。
4. 模型选择只看validation；确定模型和参数后，最终test只报告一次。若根据test结果修改模型，原test降为validation并补新的独立test。
5. 每台生成：reference、像素处理说明、intrinsics（需要时）、geometry快照、带集合标签的原始点、模型参数、有效区域、误差图、验证报告、epoch清单。文件按session存档，正式版本不通过覆盖旧文件发布。

## 5. 验收与失败分支

下面是建议项目门槛，不是实测成绩或通用行业标准。误差定义为预测与实测地面坐标的二维欧氏距离；RMSE按独立位置计算，同一点多帧不增加位置样本数。

| 相机 / 区域 | 建议最终测试门槛 | 未通过怎么办 |
|---|---|---|
| CH05–08目标平面 | RMSE≤3 cm；最大误差≤5 cm | 先查内外角对应、平面高度、箱体位姿、玻璃/畸变；补点或限制区域 |
| CH03/04各使用区域 | RMSE≤15 cm；远场最大误差≤20 cm | 先查内参、坐标和点覆盖；补远场点；允许只发布通过的近场区域 |
| CH01/02中部 | RMSE≤10 cm | 分区/补点，保留失败区域mask |
| CH01/02边缘与接缝 | 各区RMSE≤20 cm；报告最大误差 | 点不够则标为未验证，不能靠其他区域平均误差代替 |

每个单独宣称达标的区域至少3个独立最终测试位置；不足就增补整体点数。小样本只报告每点误差、RMSE和最大误差，不声称得到可靠P95分布。网格图必须通过，但图好看不能替代数值测试；poly的独立拟合逆映射也不是精确逆，尤其不能只靠逆向网格评价正向精度。

验收状态为 accepted / provisional / failed / pending，并允许逐台、逐区域发布。无法采到箱内材料不阻碍已通过的室外相机完成。

## 6. 时间、轨迹与有效期（空间标定通过后）

- 显式保存带时区的绝对时间、源片段起点、裁切偏移、帧PTS；抽样核对跨相机可见同步事件或已有LED事件。不能假定文件名秒数就是曝光时刻。目标时间误差由允许的空间误差/动物速度确定。
- 定义统一动物位置，分别验证侧视接地点近似与俯视躯干位置；用少量人工标注样本评估检测定位误差，和静态标定误差分开报告。
- WISER只读备份快照，先检查单位、轴向、原点、标签高度和时间差，再做静态和动态对照。残差包含两系统误差；有跨相机身份对应之后才能按局部精度选择同一动物的相机来源。
- 集成先用短片段；检查缺失标定、NaN、区域mask、时间解析失败是否明确失败，再扩展到共同的一小时。8路CSV能拼接不等于8路已标定或已完成身份融合。
- epoch保存reference时间、几何版本、光学/像素设置、验证时间、已证实的有效区间。碰撞或配置改变先标suspect；历史录像能否复用需用对应时期静态结构验证，不能用当前标定自动覆盖过去数月。

## 7. 实施工作清单（尚未执行）

分析端：补验证集合与误差报告、混合平面/退化检查、像素变换校验、缺失内参拒绝、版本化输出；如果启用PnP则修正K/D使用；补箱内局部坐标变换及区域mask；必要时实现全景分区模型；时间解析不得静默退回0。录制端：本README的epoch/事件/采集台账和本审计计划。

预计安排：第1天桌面准备及CH03流程预检；第2天半天现场及闭合片段检查；第3天CH03/04与箱体；第4天全景和选模；第5天独立验收、报告及短片段集成。现场失败、不可进入箱体或某区域点不够时，明确安排补采，不以修改门槛完成排期。

参考：OpenCV [homography](https://docs.opencv.org/4.13.0/d9/dab/tutorial_homography.html)说明平面映射；[PnP](https://docs.opencv.org/4.13.0/d5/d1f/calib3d_solvePnP.html)说明内参/畸变输入与IPPE共面条件。上述排期、点数和阈值是针对本项目的执行建议。
