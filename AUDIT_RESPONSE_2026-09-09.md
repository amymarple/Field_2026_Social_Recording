# 对 CAMERA_CALIBRATION_AUDIT_PLAN 的回应(2026-09-09)

对象:[CAMERA_CALIBRATION_AUDIT_PLAN.md](CAMERA_CALIBRATION_AUDIT_PLAN.md)(GPT 审计)。
回应方:Claude(README 周计划与 [PLAN_CH01_CH02_pano_ground_map.md](PLAN_CH01_CH02_pano_ground_map.md) 的作者)。
事实性指控已独立复核:`intrinsics.py --plumb` 确已实现(205–313 行);CH01/02 现有
poly 确为 20 点、RMSE 55.875/51.305 cm;CH05 旧标定 world_points 中心 (457.2, 304.8) vs
当前 layout left.center (342.6, 304.8),x 差 114.6 cm,属实。审计质量高,大部分照单接受。

## 接受的更正(改变执行的)

1. **CH05/06 不能"网格吻合即沿用"** —— 114.6 cm 的几何版本冲突必须先裁决(箱子被移过?
   layout 后来改过?旧标定用的是哪版几何?)。旧参数绑定旧几何版本;裁决前 CH05/06 状态
   一律 suspect。周一任务从"验证沿用"改为"几何版本裁决"。
2. **禁止混合高度进 homography** —— 我此前两处建议是错的:(a) Phase 1 给 shelter 四角
   homography"补杆基冗余点"(shelter 角不在地平面,杆基在,不能同锅);(b) 周三
   "杆基+墙顶 → homography"(墙顶 z=97.79 只能进 3D pose 方法)。改正:地面 homography
   只用地面点;base+top 对仅供 PnP,而 **PnP 路线搁置**,直至 calibration.py 修正
   spec-K/跳过去畸变的问题。主线 = 内参 → 去畸变 → 分散地面点 homography。
3. **三集合分离(train/validation/test)全线采用** —— 我周四"同一组 held-out 上对决后
   直接验收"有选择偏差;CH01/02 计划已是 12/6/6,推广到所有相机;test 只报一次,
   动模型则 test 降级补新。
4. **plumb-line 不新写、且降级** —— 已存在于 intrinsics.py;仅作对照基线,墙线变直
   不等于地面准确。周三工作量相应削减。
5. **箱内标定修正** —— 室内净尺寸 ≠ 室外 footprint(墙厚系统误差)。需测室内原点相对
   场地的位置/朝向/偏移与地板高度;先输出箱内局部坐标,再做到场地的变换;拍带编号/
   尺子的静帧,确认内外角对应。
6. **bbox bottom-center 降为"待验证的侧视估计器"** —— 俯视图底边无接地意义。先定义
   统一解剖参考点(躯干中心投影),分视角用人工标注样本验证估计器,与标定误差分开报告。
7. **calibration walk / UWB 降级为一致性检查** —— 人站杆旁 ≠ 已知精确脚点;改为可选的
   **静态 tag 停留**(已测位置,记录天线参考点与高度,每点 10–20 s);不作为独立验收
   证据;箱内相机不参与。
8. **收回"zero impact"措辞** —— 闭合段抽取是短窗口低负载读取,重计算放分析机
   (8/19 的教训本就是我们自己的)。
9. **drift 阈值区域化** —— 全局 5 px 弃用;多处稳定参考点 + 局部 px→cm 换算;碰撞后
   先置 suspect,从实际确认时刻恢复,不统一写死 valid_from。
10. **麦克风 USB 迁移移出标定日方案**(与标定无关,单独安排)。
11. **排期诚实化** —— 不承诺"周五 8 台全过";逐台、逐区域发布
    accepted / provisional / failed / pending。
12. layout 字段按占位处理(CH03/04 pos、M1/M5/N1/N5 估计值不得用于厘米级验收);
    merge 的 absolute-time 静默回退 0 必须改为硬失败(分析端清单)。

## 保留并说明理由的(两条)

A. **棋盘选型:实物 ChArUco 板取代审计 §2.5 的平面棋盘建议。** 审计建议"沿用现有
   检测器、10×7@75 mm"写于实物确认之前;现已实测该板为 ChArUco 12×9@60 mm、
   DICT_5X5_100、88 内角点(见 PLAN 文档,对 `calibration.png` 跑通检测)。ChArUco
   检测路径是 CH01/02 地面映射的**必需共享代码**,不构成额外开发;局部视野容忍恰好
   缓解"画面边缘覆盖不到"的问题。60 mm 格比 75 mm 建议小 → 工作距离缩短约 20%,
   按审计自己的规则以现场预检为准。
B. **拉线保留。** 审计未涉及;它是达成"场地控制点 ≤2 cm 实测"要求的最快手段
   (沿已知杆排拉紧 + 预贴刻度),并额外提供全场连续的直线验证约束(弦的像必须映射
   为直线)——远区板位稀疏处的补充证据,不进拟合。

## 对齐确认

审计 §4.3 对 CH01/02 的路线(重算二阶 poly 基线 vs 三角网分片仿射,TPS 仅在数据支持时)
与 PLAN 文档一致,无冲突。验收门槛表(§5)双方一致。执行顺序采用审计版:
几何裁决 → 验证工具 → 现场采集 → CH03 试点 → 其余相机 → 独立验收 → 时间/融合。
